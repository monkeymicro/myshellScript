#!bash

# wsl terminal input code. to connect vscode in windows

sudo apt update

sudo apt install -y g++ gdb make ninja-build rsync zip git snap

# snap install vscode
sudo snap install code --classic

